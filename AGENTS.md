# Session Rules

- Use Superpowers skills when appropriate, following their activation rules before task work.
- For backend/Laravel work, prefer the configured Laravel Boost MCP server and its version-aware tools before manual alternatives.
- For backend/Laravel work, use the local Laravel best-practices skill at `backend/.agents/skills/laravel-best-practices` when relevant.
- Keep caveman mode always on for normal replies: concise, compressed, technically exact.
- Temporarily use normal explanatory prose when the user asks for an explanation or when creating documentation under `docs/`.
