Labelled text field with a brand-green focus ring and an error state.

```jsx
<Input label="Email Address" type="email" />
<Input label="Password" type="password" error="Required." />
<Input label="Per-head limit (₱)" hint="Applies to the active cycle." />
```
