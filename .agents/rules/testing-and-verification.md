# Deprecated: read `.agents/rules/workflow.md`.

All TDD, test, build, report, browser-sweep, and evidence rules now live in
the single universal workflow file. This pointer has no independent rules and
is not routed by the root `AGENTS.md`.
