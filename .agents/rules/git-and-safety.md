# Deprecated: read `.agents/rules/workflow.md`.

All Git, safety, privacy, secret, migration, and destructive-action rules now
live in the single universal workflow file. This pointer has no independent
rules and is not routed by the root `AGENTS.md`.
