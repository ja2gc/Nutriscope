# Must be the PC's LAN IP address — NOT localhost or 127.0.0.1
# (Expo Go runs on your phone; localhost would point to the phone itself)
# Find your LAN IP: ipconfig (Windows) or ifconfig (Mac/Linux)
EXPO_PUBLIC_API_URL=http://192.168.1.100:8000
